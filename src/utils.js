// let bp = 0;
// let wp = 0;

// function createPieceID(piece) {
//   switch (piece.type) {
//     case "pawn":
//       const id = piece.color === "white" ? wp++ : bp++;
//       return piece.color[0] + piece.type[0] + id;
//     case "rook":
//       return piece.color == "white" ? "wr" : "br";
//   }
//   return wp++;
// }

// export { createPieceID };
